name: "autoprefix-to-pr"
description: "Automatically set prefix to pr title "
runs:
  using: "node16"
  main: "dist/index.js"
inputs:
  repo-token:
    permissions: "write-all"
    description: "repo-auto-token"
    required: true
